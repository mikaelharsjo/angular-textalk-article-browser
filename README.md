# angular-textalk-article-browser

An angular web app that uses textalks json-rpc service to show articles.

## Run the Application

To start the webserver.

```
npm start
```

Now browse to the app at `http://localhost:8000/app/index.html`.


## Running Unit Tests

```
npm test
```
